[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
apache-airflow = ">=1.10"
pyyaml = "*"  # Use whatever version Airflow has

[requires]
python_version = "3.7"
